# Provenance of files in this folder

## `yaml-conventions.md`

Copied from `/root/.claude/skills/powerapps-yaml/references/yaml-conventions.md`
— the `powerapps-yaml` skill as installed in the Claude Code environment.
278 lines. Self-describes as "the build contract for pasteable `pa.yaml` in this
environment", "empirically verified against Power Apps Studio".

**Unconfirmed:** whether this is the document `PROJECT-BRIEF.md` refers to as
`PowerApps_YAML_Conventions.md`, or a derivative of it. Not renamed, so the
original filename is preserved. Vin to confirm.

**Do not double-count.** A byte-identical copy also sits at
`source-artifacts/existing-skills/powerapps-yaml/references/yaml-conventions.md`.
Two copies of one document are one source, not two agreeing sources. Session 1's
reconciliation must treat them as a single Rank 2 input.

**Overlap to expect:** this document is simultaneously the Rank 2 doc for Session 1's
reconciliation and part of the Task A merge audit in Session 2. A doc-vs-artifact
contradiction recorded in `ledger/02` section 3 will therefore usually surface again
as a `ledger/03` conflict. Same finding, two ledgers — not independent evidence.
