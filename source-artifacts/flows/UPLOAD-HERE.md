# Upload here: Power Automate legacy export packages

Expected: `*.zip` files — legacy import packages that **imported cleanly**.
Also drop `validate_flow.py` here if it lives outside a skill folder.
Rank 1 ground truth for Session 2 Task B.

Delete this file once real artifacts are present.
