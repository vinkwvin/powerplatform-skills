# 06 — Vin's notes (unassisted recall)

<!-- TEMPLATE — not yet filled in. Vin writes this. -->

**Status:** empty template. Rank 3 once filled.

**Why this file is deliberately empty of content.** Claude did not draft entries here, and
must not. Its value is being an *independent* recollection that can corroborate or
contradict the artifacts. A pre-filled draft would anchor recall and turn an independent
source into an echo of what Claude already inferred — the same circular-evidence problem
the build-chat harvest prompt guards against.

**Before you write:** don't re-read `yaml-conventions.md`, `ledger/01`, or the 17
`.pa.yaml` files. Write what you remember. Gaps are fine and expected — "I remember this
kept happening but not the details" is a useful entry, because Session 2 can check it
against the artifacts.

**How to write it:** bullets, fragments, Thai or English or both. Do not polish. Ten honest
lines beat two pages of reconstruction. If a section has nothing in it, delete the section
or leave it empty — empty is a real answer.

---

## 1. Things that broke most often

The failures you hit repeatedly. For each, whatever you can recall of:

- what broke
- roughly how many times / how often
- what the fix turned out to be
- whether you ever found out *why*

## 2. Things you found yourself correcting Claude on repeatedly

Corrections you had to repeat are the strongest signal in this project — the brief promotes
a rule to inline status at "corrected 3+ times". Rough counts are fine.

## 3. Rules you're confident about but never wrote down

Things you now do automatically that aren't in any doc.

## 4. Approaches you tried and abandoned

And the reason, so a skill doesn't re-propose them.

## 5. Power Automate

You've said no flow has been built yet. Anything you know or suspect about flows from
elsewhere goes here, clearly marked as expectation rather than experience — Session 3 will
grade it `confidence: low` and Session 8 will check it against a real export.

## 6. SharePoint lists

Column types, naming, cross-list references, values a column rejected, anything about the
38-field `New_Request` list.

## 7. Manuals

What made the 360 manuals work or not work for their readers. What you'd do differently.
Whether the two-manual split (full admin / user-friendly) is fixed or negotiable.

## 8. Anything else

Including things you suspect but can't confirm. Mark them as suspicions and they'll be
graded as such, not discarded.
